<%= form_with(model: game, local: true) do |form| %>



  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>

  <% end %>

<div class="logformbox">
    <div class="field">
      <%= form.label :title %><br />
      <%= form.text_field :title, autofocus: true %>
    </div>




    <div class="field">
      <%= form.label :publisher %>
      <%= form.collection_select(:publisher_id, Publisher.all, :id,  :publisher) %>
    </div>

        <div class="field">
      <%= form.label :genre %>
      <%= form.collection_select(:genre_id, Genre.all, :id,  :genre) %>
    </div>


<% form.fields_for :game do |p| %>
<% end %>


    <div class="field"> 
      <%= form.label :picture %><br />
      <%= form.file_field :picture, accept: 'image/png, image/jpg, image/jpeg'%>
    </div>


    <div class="actions">
      <%= form.submit %>
    </div>
  </div>
<% end %>
